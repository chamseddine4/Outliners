{% extends 'base/base.html.twig ' %}


{% block titre %}
Profil
{% endblock %} 



    {% block Inscription %}
             

              </a>
              {%endblock%}
              {% block Login %}
          
              {%endblock%}
 

{% block section%}

  {%endblock%}

{% block corp%}




<div class="container rounded bg-silver mt-5 mb-5">
    <div class="row">
        <div class="col-md-3 border-right">
            
                 </div>
        <div class="col-md-5 border-right">
            <div class="p-3 py-5">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h4 class="text-right" style="color:orange">Reclamation</h4>
                </div>
                <div class="row mt-2">




                    <div class="col-md-6"><label class="labels">Nom </label><output type="text" class="form-control"> {{ Reclamation.nom }}</div>

                    <div class="col-md-6"><label class="labels">Prenom</label><output type="text" class="form-control" >{{Reclamation.prenom}}</div>
                </div>
                <div class="row mt-3">
                    <div class="col-md-12"><label class="labels">Email</label><output type="text" class="form-control">{{Reclamation.email}}</div>
                  </div>
                <div class="row mt-3">
                       <div class="col-md-12"><label class="labels">Message</label><output type="email" class="form-control" >{{Reclamation.contenu}}</div>
                  </div>
               <div class="row mt-3">

             <img class="card-img-top img-fluid" src="{{ asset('uploads/hotel_image/'~{{Reclamation.image}}) }}" alt="Card image cap">
                      </div>      
                 </div>
        <div class="row mt-3">
    <span style="margin-left:4%"> Date de Creation :
       &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <span style="color:red">{{Reclamation.createdAt | date('d/m/Y')}}</span>
          </div>
                  
   <br><br>

        </div>
   
    </div>               
</div>
</div>
</div>
 
              {%endblock%}
              
{% block footer%}

{%endblock%}

